/**
 * Template class for determining matches.
 * 
 * @class
 */
export default class Matchmaker {
  /**
   * Find a match with the given number of players per team.
   * 
   * @param {Number} playersPerTeam  The number of players required in each team.
   * @return {Match} an appropriate match or null if there is no appropriate match
   */
  findMatch(playersPerTeam) {
  }

  /**
   * Add a player for matching.
   * @param {*} player 
   */
  enterMatchmaking(player) {
  }
}